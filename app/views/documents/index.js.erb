$("tbody").replaceWith("<tbody><%= j render partial: "documents/document", collection: @documents.select { |document| document.category == @category } %></tbody>");


setSortingDirectionParamAndClass()
<%# I'd like to add the code between the comments to a different file, so that this file is organized better %>
function setDateSortDirectionToAscending(){
  $("#sort-date").replaceWith("<%= j link_to "Date", documents_path(sort: "created_at", direction: "ASC", category: @category.to_s), class:"created_at", id:"sort-date", remote: true %>");
};

function setDateSortDirectionToDescending(){
  $("#sort-date").replaceWith("<%= j link_to "Date", documents_path(sort: "created_at", direction: "DESC", category: @category.to_s), class:"created_at desc", id:"sort-date", remote: true %>");
};

function setNameSortDirectionToAscending(){
  $("#sort-name").replaceWith("<%= j link_to t('.name'), documents_path(sort: "filename", direction: "ASC", category: @category.to_s), class:"filename", id:"sort-name", remote: true %>");
};

function setNameSortDirectionToDescending(){
  $("#sort-name").replaceWith("<%= j link_to t('.name'), documents_path(sort: "filename", direction: "DESC", category: @category.to_s), class:"filename desc", id:"sort-name", remote: true %>");
};

function setSortingDirectionParamAndClass(){
  <% if  @sorting_params.include?("created_at") %> 
    if (document.querySelector("#sort-date").classList.contains("desc")){
      setDateSortDirectionToAscending();
    } else {
      setDateSortDirectionToDescending();
    };
    setNameSortDirectionToAscending();
  <% elsif @sorting_params.include?("filename") %>
    if (document.querySelector("#sort-name").classList.contains("desc")){
      setNameSortDirectionToAscending();
    } else {
      setNameSortDirectionToDescending();
    };
    setDateSortDirectionToAscending();
  <% else %>
    setNameSortDirectionToDescending();
  <% end %>
}
<%# I need to find a way to require .js.erb in this file so all I'll need to do is execute the function%>

