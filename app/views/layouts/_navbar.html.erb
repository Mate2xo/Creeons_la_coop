<nav class="navbar navbar-expand-lg navbar--bold navbar--bb-1px navbar-light bg-default ">
    <div class="container navbar-container">
        <!-- Brand/Logo -->
        <div class="block-logo">
            <%= link_to (image_tag("clac.png", :class => "navbar__clac-logo", :alt => "clac")), root_path %>
        </div>

        <div class="d-inline-block">
            <!-- Navbar toggler  -->
            <button class="navbar-toggler hamburger hamburger-js hamburger--spring" type="button" data-toggle="collapse"
                data-target="#navbar_main" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
                <span class="hamburger-box">
                    <span class="hamburger-inner"></span>
                </span>
            </button>
        </div>

        <div class="collapse navbar-collapse align-items-center justify-content-end" id="navbar_main">
            <!-- Navbar search - For small resolutions -->
            <div class="navbar-search-widget b-xs-bottom py-3 d-lg-none d-none">
                <form class="" role="form">
                    <div class="input-group input-group-lg">
                        <input type="text" class="form-control" placeholder="Search for...">
                        <span class="input-group-btn">
                            <button class="btn btn-base-3" type="button">Go!</button>
                        </span>
                    </div>
                </form>
            </div>

            <!-- Navbar links -->
            <% if member_signed_in? %>
            <ul class="navbar-nav">
                <li class="nav-item dropdown megamenu">
                    <%= link_to 'Membres', members_path, class: "nav-link" %>
                </li>
                <li class="nav-item dropdown">
                    <%= link_to 'Producteurs', productors_path, class: "nav-link" %>
                </li>
                <li class="nav-item dropdown megamenu">
                    <%= link_to 'Missions', missions_path, class: "nav-link"  %>
                </li>
                <li class="nav-item dropdown megamenu">
                    <%= link_to 'Infos', infos_path, class: "nav-link"  %>
                </li>
            </ul>
            <% end %>
            <ul class="navbar-nav ml-lg-auto">
                <% if member_signed_in? %>
                <% if current_member.role == "super_admin"  %>
                <li class="nav-item">
                    <%= link_to  "gestion admin", admin_url, class:"nav-link" %>
                </li>
                <% end %>
                <li class="nav-item">
                    <%= link_to  "profil de #{current_member.id}", edit_member_path(current_member.id), class:"nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to  "Déconnection", destroy_member_session_url, method:"delete", class:"nav-link" %>
                </li>
                <% else %>
                <li class="nav-item">
                    <%= link_to "S'incrire", new_member_registration_path, class: "nav-link"  %>
                </li>
                <li class="nav-item">
                    <%= link_to "Se connecter", new_member_session_path, class: "nav-link"  %>
                    <% end %>
                </li>
            </ul>
        </div>
    </div>
</nav>