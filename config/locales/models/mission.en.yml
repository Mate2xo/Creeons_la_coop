en:
  activerecord:
    attributes:
      mission:
        cash_register_close_out_required: Cash register close out required
        duration: duration
        genres:
          event: Event
          regulated: Regulated
          standard: Standard
        max_member_count: maximum member count
        members: Participants
        min_member_count: minimum member count
        recurrent_change: Recurrent change
      mission/enrollments:
        member: participant
    errors:
      models:
        mission:
          attributes:
            duration:
              maximum: must be less than or equal to 10 hours
              minimum: must be greater than 0 minute
              multiple: must be a multiple of 90 minutes
            enrollments:
              format: '%{message}'
              invalid: One or more related enrollment will become invalid with this update
