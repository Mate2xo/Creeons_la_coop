en:
  activerecord:
    models:
      group:
        one: Group
        other: Groups
    attributes:
      group:
        name: name
        number_of_members: number of members
        manager: Manager
